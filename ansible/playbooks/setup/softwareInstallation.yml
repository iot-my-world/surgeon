---
- hosts: brain, nerve, messaging, db
  gather_facts: true
  roles:
    - installEpelRepo
  tasks:
    - name: Add Yum Packages
      become: true
      yum:
        name: "{{ yum_packages }}"
        state: present
        update_cache: yes
