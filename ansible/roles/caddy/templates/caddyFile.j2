www.spotnav.net {
        root /var/www
        gzip
        tls brbitzbussy@gmail.com
        proxy /api-1 localhost:9010
        proxy /api-2 localhost:9011
        rewrite / {
                if {path} not_match ^/api-1
                if {path} not_match ^/api-2
                to {path} /
        }
} 
www.spotnav.co.za {
        root /var/www
        gzip
        tls calvinvw@hotmail.com
        proxy /api-1 localhost:9010
        proxy /api-2 localhost:9011
        rewrite / {
                if {path} not_match ^/api-1
                if {path} not_match ^/api-2
                to {path} /
        }
}